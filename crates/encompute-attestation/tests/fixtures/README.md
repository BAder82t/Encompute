Test-only RSA keys. `google-test.pem` stands in for Google's Confidential
Space signing key (its public half is `jwks.json`, kid `test-key-1`);
`attacker-test.pem` is an unknown key. Neither protects anything.
